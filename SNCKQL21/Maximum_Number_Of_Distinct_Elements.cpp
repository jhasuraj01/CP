#include <bits/stdc++.h>
using namespace std;

int main() {
    int T, N;
    cin >> T;

    for (int t = 0; t < T; t++)
    {
        cin >> N;
        int B[N];

        for (int i = 0; i < N; i++)
        {
            cin >> B[i];
        }
    }

    return 0;
}